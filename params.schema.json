{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://example.com/2species-to-pipelines/params.schema.json",
  "title": "2species-to-pipelines parameters",
  "type": "object",
  "properties": {
    "input": {
      "type": "string",
      "description": "Path to a CSV samplesheet with headers: sample,species_a,species_b,source_a,source_b"
    },
    "outdir": {
      "type": "string",
      "description": "Directory where pipeline outputs are written"
    },
    "harmony_container": {
      "type": "string",
      "description": "Container image for the Harmony module process"
    },
    "seurat4_container": {
      "type": "string",
      "description": "Container image for the Seurat4 module process"
    },
    "fastmnn_container": {
      "type": "string",
      "description": "Container image for the fastMNN module process"
    },
    "bbknn_container": {
      "type": "string",
      "description": "Container image for the BBKNN module process"
    },
    "scanorama_container": {
      "type": "string",
      "description": "Container image for the Scanorama module process"
    }
  },
  "required": [
    "input",
    "outdir"
  ]
}
