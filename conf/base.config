process {
    withName: MAKE_RUN_METADATA {
        cpus   = 1
        memory = '512 MB'
        time   = '30m'
    }
    publishDir = [
        path: { "${projectDir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode
    ]
}
