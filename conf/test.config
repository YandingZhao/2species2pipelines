params {
    input  = "${projectDir}/assets/samplesheet.csv"
    outdir = "${projectDir}/tests/results"
}

process {
    withName: MAKE_RUN_METADATA {
        cpus   = 1
        memory = '256 MB'
        time   = '10m'
    }
}
